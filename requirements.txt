fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
huggingface_hub>=0.23
pyyaml>=6.0

# Apple Silicon only (install via the mlx extra, not this file):
#   pip install -e ".[mlx]"
# run.sh handles this automatically on darwin/arm64.
